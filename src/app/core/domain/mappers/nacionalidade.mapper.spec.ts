import { NacionalidadeMapper } from './nacionalidade.mapper';

describe('Nacionalidade', () => {
  it('should create an instance', () => {
    expect(new NacionalidadeMapper()).toBeTruthy();
  });
});
